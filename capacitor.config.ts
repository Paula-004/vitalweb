import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.vitalfood.viandas',
  appName: 'Vital Food',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
}

export default config
