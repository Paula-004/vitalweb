'use client'
import Link from 'next/link'
import { useStoreConfig } from '@/hooks/useStoreConfig'
export default function MarketingBanner({placement='catalog'}:{placement?:'home'|'catalog'|'checkout'}){const{data}=useStoreConfig(),banner=data?.banners.find(item=>item.active&&item.placement===placement);if(!banner)return null;return <section className="px-5 py-8 lg:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-5 rounded-[2rem] bg-[#e7dccb] p-7 sm:flex-row sm:items-center"><div>{banner.eyebrow&&<p className="text-[10px] font-extrabold uppercase tracking-widest text-orange">{banner.eyebrow}</p>}<h2 className="mt-1 font-display text-3xl text-forest">{banner.title}</h2>{banner.description&&<p className="mt-2 text-sm text-ink/60">{banner.description}</p>}</div>{banner.actionUrl&&banner.actionLabel&&<Link href={banner.actionUrl} className="whitespace-nowrap rounded-full bg-orange px-5 py-3 text-center text-sm font-bold text-white">{banner.actionLabel}</Link>}</div></section>}
