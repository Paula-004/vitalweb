'use client'
import Link from 'next/link'
import { useMemo, useState } from 'react'
import { MagnifyingGlassIcon, AdjustmentsHorizontalIcon } from '@heroicons/react/24/outline'
import { useCatalogData } from '@/hooks/useCatalogData'
import { DietaryTag, Product, WeekDay } from '@/types/domain'
import ProductImage from './ProductImage'

type Mode='all'|'today'|'weekly'|'promotions'
type Sort='featured'|'best'|'price-asc'|'price-desc'|'name'|'recent'
const days:WeekDay[]=['lunes','martes','miércoles','jueves','viernes','sábado','domingo']
const money=(value:number)=>'$'+value.toLocaleString('es-AR')
export default function CatalogBrowser({mode='all',categorySlug,title,eyebrow,description}:{mode?:Mode;categorySlug?:string;title:string;eyebrow:string;description:string}){
 const{data,loading,error,retry}=useCatalogData();const[search,setSearch]=useState(''),[category,setCategory]=useState('all'),[maxPrice,setMaxPrice]=useState(12000),[onlyAvailable,setOnlyAvailable]=useState(false),[onlyPromo,setOnlyPromo]=useState(false),[day,setDay]=useState<'all'|WeekDay>('all'),[tags,setTags]=useState<DietaryTag[]>([]),[sort,setSort]=useState<Sort>('featured'),[filtersOpen,setFiltersOpen]=useState(false)
 const presetCategory=data?.categories.find(item=>item.slug===categorySlug)
 const products=useMemo(()=>{if(!data)return[];let list=[...data.products];if(mode==='today'){const ids=new Set(data.dailyMenus.find(menu=>menu.active)?.items.map(item=>item.productId)??[]);list=list.filter(item=>ids.has(item.id))}if(mode==='promotions')list=list.filter(item=>item.promotionalPrice!==undefined);const categoryId=presetCategory?.id??category;if(categoryId!=='all')list=list.filter(item=>item.categoryId===categoryId);if(search.trim()){const term=search.toLowerCase();list=list.filter(item=>`${item.name} ${item.shortDescription} ${item.ingredients.join(' ')}`.toLowerCase().includes(term))}list=list.filter(item=>(item.promotionalPrice??item.price)<=maxPrice);if(onlyAvailable)list=list.filter(item=>item.available&&item.stock>0);if(onlyPromo)list=list.filter(item=>item.promotionalPrice!==undefined);if(day!=='all')list=list.filter(item=>item.availableDays.includes(day));if(tags.length)list=list.filter(item=>tags.every(tag=>item.dietaryTags.includes(tag)));return list.sort((a,b)=>sortProducts(a,b,sort))},[data,mode,presetCategory?.id,category,search,maxPrice,onlyAvailable,onlyPromo,day,tags,sort])
 const toggleTag=(tag:DietaryTag)=>setTags(current=>current.includes(tag)?current.filter(item=>item!==tag):[...current,tag])
 return <><section className="bg-forest px-5 py-16 text-cream lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-extrabold uppercase tracking-[.2em] text-orange">{eyebrow}</p><h1 className="mt-3 max-w-3xl font-display text-4xl sm:text-6xl">{title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-cream/65">{description}</p></div></section><section className="grain min-h-[600px] px-5 py-10 lg:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><MagnifyingGlassIcon className="absolute left-4 top-1/2 h-5 -translate-y-1/2 text-forest/40"/><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Buscar platos, ingredientes..." className="w-full rounded-full border border-forest/10 bg-white py-4 pl-12 pr-5 text-sm outline-none focus:border-orange"/></label><button onClick={()=>setFiltersOpen(!filtersOpen)} className="flex items-center justify-center gap-2 rounded-full border border-forest/10 bg-white px-6 py-4 text-sm font-bold text-forest"><AdjustmentsHorizontalIcon className="h-5"/> Filtros</button><select value={sort} onChange={event=>setSort(event.target.value as Sort)} className="rounded-full border border-forest/10 bg-white px-5 py-4 text-sm font-bold text-forest"><option value="featured">Destacados</option><option value="best">Más vendidos</option><option value="price-asc">Menor precio</option><option value="price-desc">Mayor precio</option><option value="name">Nombre</option><option value="recent">Más recientes</option></select></div>{filtersOpen&&<div className="mt-4 grid gap-5 rounded-[1.5rem] bg-white p-5 shadow-soft sm:grid-cols-2 lg:grid-cols-4"><label className="text-xs font-bold text-forest">Categoría<select disabled={!!categorySlug} value={presetCategory?.id??category} onChange={event=>setCategory(event.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal"><option value="all">Todas</option>{data?.categories.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs font-bold text-forest">Precio hasta {money(maxPrice)}<input type="range" min="2000" max="12000" step="500" value={maxPrice} onChange={event=>setMaxPrice(Number(event.target.value))} className="mt-4 w-full accent-orange"/></label><label className="text-xs font-bold text-forest">Día<select value={day} onChange={event=>setDay(event.target.value as 'all'|WeekDay)} className="mt-2 w-full rounded-xl border p-3 font-normal"><option value="all">Cualquier día</option>{days.map(item=><option key={item} value={item}>{item}</option>)}</select></label><div className="space-y-2 text-xs"><Check label="Sólo disponibles" checked={onlyAvailable} onChange={setOnlyAvailable}/><Check label="En promoción" checked={onlyPromo} onChange={setOnlyPromo}/>{(['vegetariano','vegano','sin-tacc'] as DietaryTag[]).map(tag=><Check key={tag} label={tag==='sin-tacc'?'Sin TACC':capitalize(tag)} checked={tags.includes(tag)} onChange={()=>toggleTag(tag)}/>)}</div></div>}{loading?<Status title="Cargando catálogo..."/>:error?<Status title="No pudimos cargar los productos" text={error} action={retry}/>:products.length===0?<Status title="No encontramos resultados" text="Probá cambiando o quitando alguno de los filtros."/>:<><p className="mt-8 text-xs font-bold text-forest/50">{products.length} opciones</p><div className="mt-4 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{products.map(product=><ProductCard key={product.id} product={product} category={data?.categories.find(item=>item.id===product.categoryId)?.name??'Producto'}/>)}</div></>}</div></section></>
}
function ProductCard({product,category}:{product:Product;category:string}){const state=getAvailability(product);return <Link href={`/producto/${product.slug}`} className="group overflow-hidden rounded-[1.75rem] bg-white shadow-soft hover:-translate-y-1"><div className="relative"><ProductImage src={product.imageUrl} alt={product.name} className="aspect-[4/3]"/><div className="absolute left-3 top-3 flex flex-wrap gap-2">{product.promotionalPrice&&<Badge label="Promoción" orange/>}<Badge label={state.label}/></div></div><div className="p-5"><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-orange">{category}</p><h2 className="mt-2 text-lg font-extrabold leading-tight text-forest">{product.name}</h2><p className="mt-2 line-clamp-2 h-10 text-xs leading-5 text-ink/60">{product.shortDescription}</p><div className="mt-4 flex items-end gap-2">{product.promotionalPrice?<><b className="text-lg text-orange">{money(product.promotionalPrice)}</b><del className="pb-0.5 text-xs text-ink/40">{money(product.price)}</del></>:<b className="text-lg text-forest">{money(product.price)}</b>}</div></div></Link>}
function getAvailability(product:Product){if(!product.available)return{label:product.availableDate?'Próximo menú':'No disponible'};if(product.stock===0)return{label:'Agotado'};if(!product.availableDays.includes('martes'))return{label:'No disponible hoy'};if(product.stock<=5)return{label:'Pocas unidades'};return{label:'Disponible'}}
function Badge({label,orange=false}:{label:string;orange?:boolean}){return <span className={`rounded-full px-3 py-1 text-[9px] font-extrabold uppercase tracking-wide ${orange?'bg-orange text-white':'bg-cream/95 text-forest'}`}>{label}</span>}
function Check({label,checked,onChange}:{label:string;checked:boolean;onChange:(checked:boolean)=>void}){return <label className="flex items-center gap-2"><input type="checkbox" checked={checked} onChange={event=>onChange(event.target.checked)} className="accent-orange"/>{label}</label>}
function Status({title,text,action}:{title:string;text?:string;action?:()=>void}){return <div className="mt-10 rounded-[1.75rem] bg-white p-12 text-center shadow-soft"><h2 className="font-display text-2xl text-forest">{title}</h2>{text&&<p className="mt-2 text-sm text-ink/60">{text}</p>}{action&&<button onClick={action} className="mt-5 rounded-full bg-orange px-5 py-3 text-sm font-bold text-white">Reintentar</button>}</div>}
function sortProducts(a:Product,b:Product,sort:Sort){if(sort==='best')return Number(b.bestSeller)-Number(a.bestSeller);if(sort==='price-asc')return(a.promotionalPrice??a.price)-(b.promotionalPrice??b.price);if(sort==='price-desc')return(b.promotionalPrice??b.price)-(a.promotionalPrice??a.price);if(sort==='name')return a.name.localeCompare(b.name);if(sort==='recent')return b.createdAt.localeCompare(a.createdAt);return Number(b.featured)-Number(a.featured)||a.displayOrder-b.displayOrder}
function capitalize(value:string){return value.charAt(0).toUpperCase()+value.slice(1)}
